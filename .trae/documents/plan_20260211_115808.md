Baik, saya akan mengimplementasikan fitur screenshot dengan tampilan **Desktop** menggunakan `puppeteer`.

**Rencana Implementasi:**

1. **Dependency**: Menambahkan `puppeteer` ke `package.json`.
2. **Fitur Bot**:

   * Command baru: `/ss <url>` (Contoh: `/ss https://google.com`).

   * Bot akan meluncurkan browser (headless), mengatur viewport ke ukuran Desktop (1

