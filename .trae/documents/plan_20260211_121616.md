I will modify `bot.js` to implement the requested UI-based screenshot feature and fix the dependency error.

1.  **Create `screenshotWizard` Scene**:
    *   **Step 1**: Prompt the user to send a URL (e.g., "📸 Silakan kirimkan URL website...").
    *   **Step 2**: Receive the URL, validate it, and attempt to take a screenshot using Puppeteer.
    *   **Error Handling**: If the "missing library" error occurs, it will display the error message along with a **"🛠️ Fix Dependencies"** button.

2.  **Add "Fix Dependencies" Action**:
    *   Implement `bot.action('install_puppeteer_deps')` to run `sudo apt-get install` for the missing libraries (`libnss3`, etc.) on the VPS.

3.  **Update Bot Menu & Configuration**:
    *   Add a **"📸 Screenshot Web"** button to the "Fitur Lainnya" (`moreMenu`) menu.
    *   Register the new `screenshotWizard` in the `Scenes.Stage` configuration.
    *   Add the `start_screenshot` action to trigger the wizard.

This approach solves the original `libnss3` error by providing a one-click fix and fulfills the new request for a button-based workflow.